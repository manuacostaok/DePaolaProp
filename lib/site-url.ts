// TODO: reemplazar por el dominio propio (depaolapropiedades.com) en la Fase 17.
// Mientras tanto usa la URL de Vercel para que sitemap/robots/schema.org tengan
// URLs absolutas válidas ya desde ahora.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://de-paola-prop.vercel.app";
